## v0.4.0

- 新增可点击的四分区键盘，调整分区边界与按键布局。
- 合并内置与自定义灯光预设，支持修改、改名、另存和删除；内置预设默认亮度为 100%。
- 精简中英文界面，操作提示移至底部状态栏，补充 EC 模式标志说明。
- 性能模式统一命名为“狂暴”。
- EC 模块内嵌，发行包不再需要独立的 `ec` 文件夹。

### English

- Add a clickable four-zone keyboard with corrected zone boundaries and key layout.
- Unify built-in and custom lighting presets with edit, rename, save-as and delete; built-in presets default to 100% brightness.
- Simplify both interfaces, move feedback to the status bar, and explain EC mode flags.
- Rename the Chinese Performance mode to “狂暴”.
- Embed the EC module; the portable package no longer needs a separate `ec` folder.
