## v0.3.0

- 移除 Eco，保留默认、狂暴 / 性能、酷冷三种 BIOS 模式。
- 新增 EC 回读，显示实际性能状态，读取失败显示未知。
- 新增 PawnIO 驱动一键安装，自动复用已有兼容驱动。
- 移除正常使用对 NVIDIA DLL 的依赖。
- 新增硬件操作防连点，修复快速刷新出错。
- 缩短键盘应用等待，语言切换和颜色编辑不再阻塞。
- 移除界面的刷新率和读取时间。
- 配置改存程序旁的 `data` 文件夹，支持导入旧预设。
